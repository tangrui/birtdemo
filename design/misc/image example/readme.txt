http://www.birt-exchange.com/devshare/designing-birt-reports/343-birt-web-2-0-image-example/

Simple example using the document.getElementById to replace an image
This is a simple example that shows how to use the Bookmark property to hold a handle to an image. You can then reference that image using the document.getElementById method to manipulate the image. In this example, the image source is changed.

This example was created with BIRT 2.2.1

